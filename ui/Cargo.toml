[package]
name = "morpheus-ui"
version.workspace = true
edition.workspace = true
description = "Standalone no_std bare-metal UI framework — TTY shell + windowed applications on raw framebuffers"
license = "MIT OR Apache-2.0"
rust-version = "1.75"

[features]
default = []

[dependencies]
