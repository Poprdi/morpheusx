[package]
name = "morpheus-cli"
version.workspace = true  
edition.workspace = true

[[bin]]
name = "morpheus-cli"
path = "src/main.rs"

[dependencies]
morpheus-helix = { path = "../helix" }
gpt_disk_io = "0.16"
gpt_disk_types = "0.16"
